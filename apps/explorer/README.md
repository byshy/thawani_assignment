# explorer

Rick and Morty Explorer — Thawani Flutter assignment

## Run

From `apps/explorer`:

```bash
flutter pub get

flutter run --flavor dev
flutter run --flavor prod
```

Both flavors use the public Rick and Morty API. They differ by app name, Android application id / iOS bundle id, and the in-app environment label.

```bash
flutter analyze
flutter test
```
