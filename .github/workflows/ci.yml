# Analyze and test changed SDK packages and Explorer.
# Changes under .github/ or a manual workflow_dispatch run every SDK and Explorer.
name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  changes:
    name: Detect changed packages
    runs-on: ubuntu-latest
    outputs:
      packages: ${{ steps.select.outputs.packages }}
      any: ${{ steps.select.outputs.any }}
      explorer: ${{ steps.select.outputs.explorer }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Test change detection
        working-directory: .github/scripts
        run: python3 -m unittest -v

      - name: Select packages
        id: select
        env:
          EVENT_NAME: ${{ github.event_name }}
          EVENT_BEFORE: ${{ github.event.before }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.sha }}
        run: python3 .github/scripts/select_sdk_packages.py

  analyze-and-test:
    name: ${{ matrix.package }}
    needs: changes
    if: needs.changes.outputs.any == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        package: ${{ fromJSON(needs.changes.outputs.packages) }}
    defaults:
      run:
        working-directory: sdk/${{ matrix.package }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: "3.41.6"
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --fatal-warnings

      - name: Test
        run: flutter test

  explorer:
    name: explorer
    needs: changes
    if: needs.changes.outputs.explorer == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: apps/explorer
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: "3.41.6"
          cache: true

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze --fatal-warnings

      - name: Test
        run: flutter test

  ci:
    name: CI
    needs: [changes, analyze-and-test, explorer]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Check CI result
        run: |
          if [[ "${{ needs.changes.result }}" != "success" ]]; then
            echo "Change detection failed"
            exit 1
          fi
          if [[ "${{ needs.changes.outputs.any }}" == "true" && "${{ needs.analyze-and-test.result }}" != "success" ]]; then
            echo "SDK analyze/test failed (${{ needs.analyze-and-test.result }})"
            exit 1
          fi
          if [[ "${{ needs.changes.outputs.explorer }}" == "true" && "${{ needs.explorer.result }}" != "success" ]]; then
            echo "Explorer analyze/test failed (${{ needs.explorer.result }})"
            exit 1
          fi
          if [[ "${{ needs.changes.outputs.any }}" != "true" && "${{ needs.changes.outputs.explorer }}" != "true" ]]; then
            echo "No SDK or Explorer changes; skipped analyze and tests."
            exit 0
          fi
          echo "Selected packages passed analyze and tests."
