name: thawani_ui
description: Internal design tokens and reusable presentation widgets.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
