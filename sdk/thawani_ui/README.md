# thawani_ui

Internal design tokens and reusable presentation widgets for Explorer (and future apps).

Presentation-only — no Dio, Hive, or repository imports.

```dart
import 'package:thawani_ui/thawani_ui.dart';

MaterialApp(
  theme: ThawaniTheme.light(),
  home: ...,
);
```
